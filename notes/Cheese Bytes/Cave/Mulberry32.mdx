import dedent from "dedent";
import HistogramGenerator from "../../../apps/cheesebytes-web/src/components/cave/Mulberry32.tsx";
import PyodideCodeRunner from "../../../apps/cheesebytes-web/src/components/PyodideCodeRunner.tsx";

export const mulberry32PythonCode = dedent`
	from typing import Callable

    def mulberry32(seed: int) -> Callable[[], float]:
        seed &= 0xFFFFFFFF

        def _aux() -> float:
            nonlocal seed
            seed = (seed + 0x6D2B79F5) & 0xFFFFFFFF
            t = seed
            t = ((t ^ (t >> 15)) * (t | 1)) & 0xFFFFFFFF
            t ^= (t + ((t ^ (t >> 7)) * (t | 61) & 0xFFFFFFFF)) & 0xFFFFFFFF
            return ((t ^ (t >> 14)) & 0xFFFFFFFF) / 4294967296.0

        return _aux

    rng = mulberry32(124);

`;

<PyodideCodeRunner client:only="react" initialCode={mulberry32PythonCode} />

<HistogramGenerator client:only="react" />
